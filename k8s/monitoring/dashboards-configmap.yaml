apiVersion: v1
kind: ConfigMap
metadata:
  name: 2k-scout-overview-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  2k-scout-overview.json: |
    {
      "title": "2K Scout - Service Overview",
      "uid": "2k-scout-overview",
      "schemaVersion": 39,
      "timezone": "browser",
      "time": { "from": "now-1h", "to": "now" },
      "refresh": "30s",
      "templating": {
        "list": [
          {
            "name": "datasource",
            "type": "datasource",
            "query": "prometheus",
            "hide": 0,
            "current": {}
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "Logs",
          "type": "text",
          "gridPos": { "h": 3, "w": 24, "x": 0, "y": 0 },
          "options": {
            "mode": "markdown",
            "content": "Application logs (every `2k-scout` pod's stdout, forwarded by Fluent Bit) live in **Loki**, queryable right here in Grafana — no separate login. Go to **Explore** (left sidebar), pick the **Loki** datasource, and run `{job=\"fluent-bit\"}` (the input is already scoped to 2k-scout containers only, so there's no separate namespace label to filter on) — add `| json | kubernetes_container_name=\"gateway\"` to narrow to one service. See also: [SLOs & Error Budget](/d/2k-scout-slo) · [AI Cost & Quality](/d/2k-scout-ai)."
          }
        },
        {
          "id": 2,
          "title": "Traffic",
          "type": "row",
          "collapsed": false,
          "gridPos": { "h": 1, "w": 24, "x": 0, "y": 3 },
          "panels": []
        },
        {
          "id": 3,
          "title": "Services Up",
          "type": "stat",
          "datasource": "${datasource}",
          "gridPos": { "h": 6, "w": 6, "x": 0, "y": 4 },
          "targets": [
            {
              "expr": "sum(up{job=~\"gateway|team-service|ai-service|auth-service\"})",
              "legendFormat": "up"
            },
            {
              "expr": "count(up{job=~\"gateway|team-service|ai-service|auth-service\"})",
              "legendFormat": "total"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "red", "value": null },
                  { "color": "green", "value": 4 }
                ]
              }
            }
          }
        },
        {
          "id": 4,
          "title": "Total Request Rate (req/s)",
          "type": "stat",
          "datasource": "${datasource}",
          "description": "Demand context for every other panel on this dashboard — 5% errors means something very different at 2 req/s (one dev testing) than at 2000 req/s.",
          "gridPos": { "h": 6, "w": 6, "x": 6, "y": 4 },
          "targets": [
            {
              "expr": "sum(rate(http_request_duration_seconds_count{job=~\"gateway|team-service|ai-service|auth-service\"}[5m]))",
              "legendFormat": "req/s"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "reqps" }
          }
        },
        {
          "id": 5,
          "title": "Request Rate by Service",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 6, "w": 12, "x": 12, "y": 4 },
          "targets": [
            {
              "expr": "sum(rate(http_request_duration_seconds_count[5m])) by (job)",
              "legendFormat": "{{job}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "reqps" }
          }
        },
        {
          "id": 6,
          "title": "Latency",
          "type": "row",
          "collapsed": false,
          "gridPos": { "h": 1, "w": 24, "x": 0, "y": 10 },
          "panels": []
        },
        {
          "id": 7,
          "title": "p50 / p95 / p99 Latency (all services)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Percentiles, not an average — p50 is the typical request, p99 is what your worst-off users see. A flat p50 with a climbing p99 is a real, hidden problem an average would completely mask.",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 11 },
          "targets": [
            {
              "expr": "histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\"}[5m])) by (le))",
              "legendFormat": "p50"
            },
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\"}[5m])) by (le))",
              "legendFormat": "p95"
            },
            {
              "expr": "histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\"}[5m])) by (le))",
              "legendFormat": "p99"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "s" }
          }
        },
        {
          "id": 8,
          "title": "Successful vs. Failed Request Latency (p95)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "A fast error is not the same problem as a slow success — averaging them together hides both. 'failed' is 4xx+5xx responses; 'successful' is 2xx/3xx. A slow success p95 usually means real backend/DB/downstream latency; a fast-failing error usually means validation or auth rejecting requests early. Watch for the failed line dropping toward zero — that can mean errors are being rejected so fast they're barely being timed at all, which is its own signal.",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 11 },
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\", status_code=~\"2..|3..\"}[5m])) by (le))",
              "legendFormat": "successful (2xx/3xx)"
            },
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\", status_code=~\"4..|5..\"}[5m])) by (le))",
              "legendFormat": "failed (4xx/5xx)"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "s",
              "custom": { "lineStyle": { "fill": "solid" } }
            },
            "overrides": [
              {
                "matcher": { "id": "byName", "options": "failed (4xx/5xx)" },
                "properties": [{ "id": "color", "value": { "mode": "fixed", "fixedColor": "red" } }]
              }
            ]
          }
        },
        {
          "id": 9,
          "title": "p95 Latency by Service",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 19 },
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, job))",
              "legendFormat": "{{job}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "s" }
          }
        },
        {
          "id": 10,
          "title": "Apdex Score by Service",
          "type": "bargauge",
          "datasource": "${datasource}",
          "description": "Satisfied <= 500ms (matches the fast-endpoint latency SLO), tolerating <= 2.5s, frustrated beyond that. 1.0 = every request satisfied. ai-service will read structurally lower here since /api/gameplan is dominated by Claude generation time, not a bug — same reason it's excluded from the fast-latency SLO and gets its own AI Claude Latency SLO instead.",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 19 },
          "targets": [
            {
              "expr": "(sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\", le=\"0.5\"}[5m])) by (job) + (sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\", le=\"2.5\"}[5m])) by (job) - sum(rate(http_request_duration_seconds_bucket{job=~\"gateway|team-service|ai-service|auth-service\", le=\"0.5\"}[5m])) by (job)) / 2) / sum(rate(http_request_duration_seconds_count{job=~\"gateway|team-service|ai-service|auth-service\"}[5m])) by (job)",
              "legendFormat": "{{job}}"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "percentunit",
              "min": 0,
              "max": 1,
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "red", "value": null },
                  { "color": "yellow", "value": 0.7 },
                  { "color": "green", "value": 0.94 }
                ]
              }
            }
          }
        },
        {
          "id": 11,
          "title": "Errors",
          "type": "row",
          "collapsed": false,
          "gridPos": { "h": 1, "w": 24, "x": 0, "y": 27 },
          "panels": []
        },
        {
          "id": 12,
          "title": "Error Rate % by Service",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "5xx (server-caused) as a percentage of that service's own traffic, not raw count — this is the same ratio the availability SLO burn-rate alerts watch (see the SLOs & Error Budget dashboard for budget context). Clamped to 0-100%: with light test traffic and frequent pod restarts, a rate() counter-reset artifact can otherwise produce a brief nonsense spike (mathematically 5xx/total can never exceed 100%) that blows out the whole axis.",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 28 },
          "targets": [
            {
              "expr": "clamp_max(sum(rate(http_request_duration_seconds_count{job=~\"gateway|team-service|ai-service|auth-service\", status_code=~\"5..\"}[5m])) by (job) / sum(rate(http_request_duration_seconds_count{job=~\"gateway|team-service|ai-service|auth-service\"}[5m])) by (job), 1)",
              "legendFormat": "{{job}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "percentunit", "min": 0, "max": 1 }
          }
        },
        {
          "id": 13,
          "title": "Error Rate by Type: 4xx vs 5xx (all services)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Very different signals — 4xx is client-caused (bad input, expired auth token, calling an endpoint wrong) and rarely means the service itself is broken; 5xx is server-caused and is what the availability SLO actually tracks. A 4xx spike with flat 5xx usually points at the frontend or a client integration, not this backend.",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 28 },
          "targets": [
            {
              "expr": "sum(rate(http_request_duration_seconds_count{job=~\"gateway|team-service|ai-service|auth-service\", status_code=~\"4..\"}[5m]))",
              "legendFormat": "4xx (client)"
            },
            {
              "expr": "sum(rate(http_request_duration_seconds_count{job=~\"gateway|team-service|ai-service|auth-service\", status_code=~\"5..\"}[5m]))",
              "legendFormat": "5xx (server)"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "reqps" },
            "overrides": [
              {
                "matcher": { "id": "byName", "options": "5xx (server)" },
                "properties": [{ "id": "color", "value": { "mode": "fixed", "fixedColor": "red" } }]
              },
              {
                "matcher": { "id": "byName", "options": "4xx (client)" },
                "properties": [{ "id": "color", "value": { "mode": "fixed", "fixedColor": "orange" } }]
              }
            ]
          }
        },
        {
          "id": 14,
          "title": "Saturation",
          "type": "row",
          "collapsed": false,
          "gridPos": { "h": 1, "w": 24, "x": 0, "y": 36 },
          "panels": []
        },
        {
          "id": 15,
          "title": "CPU Usage by Pod (2k-scout namespace)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 37 },
          "targets": [
            {
              "expr": "sum(rate(container_cpu_usage_seconds_total{namespace=\"2k-scout\", container!=\"\", container!=\"POD\"}[5m])) by (pod)",
              "legendFormat": "{{pod}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "short" }
          }
        },
        {
          "id": 16,
          "title": "Memory Usage by Pod (2k-scout namespace)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 37 },
          "targets": [
            {
              "expr": "sum(container_memory_working_set_bytes{namespace=\"2k-scout\", container!=\"\", container!=\"POD\"}) by (pod)",
              "legendFormat": "{{pod}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "bytes" }
          }
        },
        {
          "id": 17,
          "title": "Node CPU: Requested vs Allocatable",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "The metric that would have caught the Postgres/Redis/Splunk resource crunch early — when 'requested' approaches 'allocatable', new pods start failing to schedule. Things generally degrade before they fully fail; this is the early-warning line, not the outage itself.",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 45 },
          "targets": [
            {
              "expr": "sum(kube_pod_container_resource_requests{resource=\"cpu\"})",
              "legendFormat": "requested"
            },
            {
              "expr": "sum(kube_node_status_allocatable{resource=\"cpu\"})",
              "legendFormat": "allocatable"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "short" }
          }
        },
        {
          "id": 18,
          "title": "Node Memory: Requested vs Allocatable",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Same idea for memory — this is the specific number that was maxed out during the Splunk deployment incident.",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 45 },
          "targets": [
            {
              "expr": "sum(kube_pod_container_resource_requests{resource=\"memory\"})",
              "legendFormat": "requested"
            },
            {
              "expr": "sum(kube_node_status_allocatable{resource=\"memory\"})",
              "legendFormat": "allocatable"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "bytes" }
          }
        },
        {
          "id": 19,
          "title": "Node Disk I/O",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Node-wide (this is a single-node kind cluster, so 'node' and 'cluster' are the same box), from node-exporter. Rising I/O time with flat request volume is often the first sign of a saturated disk before latency or errors move at all.",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 53 },
          "targets": [
            {
              "expr": "sum(rate(node_disk_io_time_seconds_total[5m])) by (instance)",
              "legendFormat": "{{instance}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "percentunit" }
          }
        },
        {
          "id": 20,
          "title": "Node Network Throughput",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 53 },
          "targets": [
            {
              "expr": "sum(rate(node_network_receive_bytes_total{device!=\"lo\"}[5m])) by (instance)",
              "legendFormat": "{{instance}} rx"
            },
            {
              "expr": "sum(rate(node_network_transmit_bytes_total{device!=\"lo\"}[5m])) by (instance)",
              "legendFormat": "{{instance}} tx"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "Bps" }
          }
        },
        {
          "id": 21,
          "title": "Kubernetes",
          "type": "row",
          "collapsed": false,
          "gridPos": { "h": 1, "w": 24, "x": 0, "y": 61 },
          "panels": []
        },
        {
          "id": 22,
          "title": "Pod Restarts (2k-scout namespace)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Crash-looping pods. A single restart after a deploy is normal; a climbing count on one pod while the rest are flat is the thing to chase.",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 62 },
          "targets": [
            {
              "expr": "sum(increase(kube_pod_container_status_restarts_total{namespace=\"2k-scout\"}[15m])) by (pod)",
              "legendFormat": "{{pod}}"
            }
          ]
        },
        {
          "id": 23,
          "title": "Readiness / Liveness Probe Failure Rate",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "From kubelet's own prober_probe_total metric — tells you when Kubernetes thinks a pod is unhealthy (or vice versa: a pod that's actually struggling but still passing probes won't show up here, only in the latency/error panels above). Requires the kubelet ServiceMonitor kube-prometheus-stack installs by default; if this stays empty on a real cluster while pods are visibly restarting, check that kubelet metrics are actually being scraped.",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 62 },
          "targets": [
            {
              "expr": "sum(rate(prober_probe_total{namespace=\"2k-scout\", result=\"failed\"}[5m])) by (pod, probe_type)",
              "legendFormat": "{{pod}} ({{probe_type}})"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "short" }
          }
        },
        {
          "id": 24,
          "title": "Service-Specific Signals",
          "type": "row",
          "collapsed": false,
          "gridPos": { "h": 1, "w": 24, "x": 0, "y": 70 },
          "panels": []
        },
        {
          "id": 25,
          "title": "Gateway: Request Rate by Route",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Gateway path-routes to three different services (ai-service, team-service, auth-service) — a blended request rate hides that /api/gameplan behaves nothing like /auth/login. Split by route instead of by job so each downstream's traffic shape is visible on its own.",
          "gridPos": { "h": 8, "w": 8, "x": 0, "y": 71 },
          "targets": [
            {
              "expr": "sum(rate(http_request_duration_seconds_count{job=\"gateway\"}[5m])) by (route)",
              "legendFormat": "{{route}}"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "reqps" } }
        },
        {
          "id": 26,
          "title": "Gateway: Error Rate by Route",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Tells you instantly which downstream is having a bad day — a spike isolated to /api/gameplan means ai-service (or Claude itself), not team-service or auth-service.",
          "gridPos": { "h": 8, "w": 8, "x": 8, "y": 71 },
          "targets": [
            {
              "expr": "clamp_max(sum(rate(http_request_duration_seconds_count{job=\"gateway\", status_code=~\"5..\"}[5m])) by (route) / sum(rate(http_request_duration_seconds_count{job=\"gateway\"}[5m])) by (route), 1)",
              "legendFormat": "{{route}}"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "percentunit", "min": 0, "max": 1 } }
        },
        {
          "id": 27,
          "title": "Gateway: p95 Latency by Route",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Blended latency would hide a slow ai-service call inside an otherwise-healthy average — /api/gameplan is expected to run structurally higher here than /api/teams or /auth/login (Claude generation time, not a bug).",
          "gridPos": { "h": 8, "w": 8, "x": 16, "y": 71 },
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=\"gateway\"}[5m])) by (le, route))",
              "legendFormat": "{{route}}"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "s" } }
        },
        {
          "id": 28,
          "title": "Team-Service: Cache Hit Rate",
          "type": "stat",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 8, "x": 0, "y": 79 },
          "targets": [
            {
              "expr": "sum(increase(cache_hits_total{job=\"team-service\"}[1h])) / (sum(increase(cache_hits_total{job=\"team-service\"}[1h])) + sum(increase(cache_misses_total{job=\"team-service\"}[1h])))",
              "legendFormat": "hit rate"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "percentunit", "min": 0, "max": 1 } }
        },
        {
          "id": 29,
          "title": "Team-Service: Cache Entry Age (p50/p95)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "A cache hit rate alone can't tell you if you're serving stale data. 2K ratings change across patches — a healthy hit rate with climbing age is a silent problem: correct-looking responses that are quietly out of date. Bounded by the 1h cache TTL (CACHE_TTL_SECONDS in team-service/index.js); age should never exceed that.",
          "gridPos": { "h": 8, "w": 8, "x": 8, "y": 79 },
          "targets": [
            {
              "expr": "histogram_quantile(0.50, sum(rate(cache_entry_age_seconds_bucket{job=\"team-service\"}[5m])) by (le))",
              "legendFormat": "p50"
            },
            {
              "expr": "histogram_quantile(0.95, sum(rate(cache_entry_age_seconds_bucket{job=\"team-service\"}[5m])) by (le))",
              "legendFormat": "p95"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "s" } }
        },
        {
          "id": 30,
          "title": "Team-Service: nba2kapi Upstream Error Rate",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Isolated from team-service's own http_request_duration_seconds status codes on purpose — same principle as ai-service's Claude error tracking. 'nba2kapi is down' and 'we have a bug' need different responses, and a blended error rate can't tell them apart.",
          "gridPos": { "h": 8, "w": 8, "x": 16, "y": 79 },
          "targets": [
            {
              "expr": "clamp_max(sum(rate(nba2kapi_upstream_requests_total{job=\"team-service\", outcome=\"error\"}[5m])) / sum(rate(nba2kapi_upstream_requests_total{job=\"team-service\"}[5m])), 1)",
              "legendFormat": "error rate"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "percentunit", "min": 0, "max": 1 } }
        },
        {
          "id": 31,
          "title": "Auth: Failed Login Rate",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "A spike here is a security signal (credential stuffing, a leaked password list being tried) as much as a UX one — not just 'users mistyping passwords.'",
          "gridPos": { "h": 8, "w": 6, "x": 0, "y": 87 },
          "targets": [
            {
              "expr": "clamp_max(sum(rate(http_request_duration_seconds_count{job=\"auth-service\", route=\"/auth/login\", status_code=\"401\"}[5m])) / sum(rate(http_request_duration_seconds_count{job=\"auth-service\", route=\"/auth/login\"}[5m])), 1)",
              "legendFormat": "failed login rate"
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "percentunit",
              "min": 0,
              "max": 1,
              "color": { "mode": "fixed", "fixedColor": "orange" }
            }
          }
        },
        {
          "id": 32,
          "title": "Auth: Login Latency (p95)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Login is both critical and expected to be fast — nobody tolerates a slow login the way they might tolerate a slow report generation.",
          "gridPos": { "h": 8, "w": 6, "x": 6, "y": 87 },
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=\"auth-service\", route=\"/auth/login\"}[5m])) by (le))",
              "legendFormat": "p95"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "s" } }
        },
        {
          "id": 33,
          "title": "Frontend: JS Error Rate",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Uncaught exceptions and unhandled promise rejections in the browser — reported by src/lib/metrics.js to gateway's POST /api/client-metrics. A healthy backend doesn't guarantee the SPA actually renders; this is the only panel on either dashboard that can see a JS bug in the matchup-calculation/rendering logic, since it never touches a backend endpoint at all.",
          "gridPos": { "h": 8, "w": 6, "x": 12, "y": 87 },
          "targets": [
            {
              "expr": "rate(frontend_js_errors_total{job=\"gateway\"}[5m])",
              "legendFormat": "errors/s"
            }
          ],
          "fieldConfig": {
            "defaults": { "unit": "short", "color": { "mode": "fixed", "fixedColor": "red" } }
          }
        },
        {
          "id": 34,
          "title": "Frontend: Time-to-Interactive (p95)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "Navigation start to the app's first render, reported once per page load. An approximation, not the strict web-vitals TTI — good enough to catch 'the app got a lot slower to show up,' which is what's actually worth alerting on. A healthy p95 latency on every backend panel above doesn't guarantee this looks healthy too — a large JS bundle or slow third-party font load wouldn't show up anywhere else on either dashboard.",
          "gridPos": { "h": 8, "w": 6, "x": 18, "y": 87 },
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(frontend_time_to_interactive_seconds_bucket{job=\"gateway\"}[5m])) by (le))",
              "legendFormat": "p95"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "s" } }
        }
      ]
    }
