apiVersion: v1
kind: ConfigMap
metadata:
  name: 2k-scout-ai-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  2k-scout-ai.json: |
    {
      "title": "2K Scout - AI Cost & Quality",
      "uid": "2k-scout-ai",
      "schemaVersion": 39,
      "timezone": "browser",
      "time": { "from": "now-6h", "to": "now" },
      "refresh": "30s",
      "templating": {
        "list": [
          {
            "name": "datasource",
            "type": "datasource",
            "query": "prometheus",
            "hide": 0,
            "current": {}
          }
        ]
      },
      "panels": [
        {
          "id": 1,
          "title": "Estimated Spend (last 6h)",
          "type": "stat",
          "datasource": "${datasource}",
          "gridPos": { "h": 5, "w": 8, "x": 0, "y": 0 },
          "targets": [
            { "expr": "increase(ai_estimated_cost_usd_total[6h])", "legendFormat": "USD" }
          ],
          "fieldConfig": { "defaults": { "unit": "currencyUSD", "decimals": 4 } }
        },
        {
          "id": 2,
          "title": "Cache Hit Rate",
          "type": "stat",
          "datasource": "${datasource}",
          "gridPos": { "h": 5, "w": 8, "x": 8, "y": 0 },
          "targets": [
            {
              "expr": "sum(increase(cache_hits_total{job=\"ai-service\"}[1h])) / (sum(increase(cache_hits_total{job=\"ai-service\"}[1h])) + sum(increase(cache_misses_total{job=\"ai-service\"}[1h])))",
              "legendFormat": "hit rate"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "percentunit", "min": 0, "max": 1 } }
        },
        {
          "id": 3,
          "title": "Truncated Responses (stop_reason=max_tokens, last 6h)",
          "type": "stat",
          "datasource": "${datasource}",
          "gridPos": { "h": 5, "w": 8, "x": 16, "y": 0 },
          "targets": [
            { "expr": "increase(ai_stop_reason_total{reason=\"max_tokens\"}[6h])", "legendFormat": "truncated" }
          ],
          "fieldConfig": {
            "defaults": {
              "thresholds": {
                "mode": "absolute",
                "steps": [
                  { "color": "green", "value": null },
                  { "color": "red", "value": 1 }
                ]
              }
            }
          }
        },
        {
          "id": 4,
          "title": "Token Usage (per 5m, by direction)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 5 },
          "targets": [
            {
              "expr": "sum(rate(ai_tokens_total[5m])) by (direction)",
              "legendFormat": "{{direction}}"
            }
          ]
        },
        {
          "id": 5,
          "title": "Claude Call Latency (p95/p99)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "description": "This is the one call in the app most likely to take a real, noticeable amount of time — LLM generation isn't instant. p99 matters more here than on most panels: it's the number your most-affected users actually experience, and generation time has a much fatter tail than a typical CRUD endpoint.",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 5 },
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(anthropic_request_duration_seconds_bucket[5m])) by (le))",
              "legendFormat": "p95"
            },
            {
              "expr": "histogram_quantile(0.99, sum(rate(anthropic_request_duration_seconds_bucket[5m])) by (le))",
              "legendFormat": "p99"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "s" } }
        },
        {
          "id": 6,
          "title": "Claude Errors by Type",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 13 },
          "targets": [
            {
              "expr": "sum(rate(ai_errors_total[5m])) by (type)",
              "legendFormat": "{{type}}"
            }
          ],
          "fieldConfig": {
            "defaults": { "color": { "mode": "fixed", "fixedColor": "red" } }
          }
        },
        {
          "id": 7,
          "title": "Estimated Cost Rate ($/hour)",
          "type": "timeseries",
          "datasource": "${datasource}",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 13 },
          "targets": [
            {
              "expr": "rate(ai_estimated_cost_usd_total[15m]) * 3600",
              "legendFormat": "$/hr"
            }
          ],
          "fieldConfig": { "defaults": { "unit": "currencyUSD", "decimals": 3 } }
        },
        {
          "id": 8,
          "title": "Known Gap: No Fallback on Claude Failure",
          "type": "text",
          "gridPos": { "h": 4, "w": 24, "x": 0, "y": 21 },
          "options": {
            "mode": "markdown",
            "content": "**There is currently no degraded mode.** If the Anthropic API call in `POST /api/gameplan` fails outright (rate limit, outage, timeout), the user gets a generic `500` — there's no simpler rules-based matchup summary served as a fallback. The **Claude Errors by Type** panel above tells you *that* and *why* it's failing, but every failure there is a failure the user sees, full stop. If this ever needs to be more resilient, a fallback path would need its own usage-rate metric (e.g. `ai_fallback_used_total`) so degraded-mode traffic stays visible instead of silently blending into the 'success' numbers."
          }
        }
      ]
    }
