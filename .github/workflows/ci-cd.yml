# CI/CD is handled by AWS CodePipeline triggered by AWS CodeCommit pushes.
# See buildspec-build.yml, buildspec-docker.yml, and buildspec-deploy.yml.
name: CI/CD

on:
  push:
    branches: []  # disabled — pipeline triggers on CodeCommit push via EventBridge
