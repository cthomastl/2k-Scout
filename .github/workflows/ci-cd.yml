# CI/CD is handled by AWS CodePipeline.
# See buildspec-build.yml, buildspec-docker.yml, and buildspec-deploy.yml.
name: CI/CD

on:
  push:
    branches: []  # disabled — pipeline runs in CodePipeline
